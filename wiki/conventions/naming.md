# Naming Conventions

**Files:**
- React Components: `PascalCase` (e.g., `ActiveUsersCard.jsx`)
- API Endpoints: `kebab-case` (e.g., `active-users.js`)
- Utility functions: `camelCase` (e.g., `formatDate.js`)
